#include "main.h"

/**
 * main - tasts function tha prints that if an integer is positive or negative
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
